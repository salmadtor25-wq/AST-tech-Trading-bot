# AST Capital Trading Platform — Python dependencies
# Install:  pip install -r requirements.txt

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Data / numerics
pandas>=2.0.0
numpy>=1.24.0

# MetaTrader 5 API (Windows only — comment out on Linux/Mac for backtest use)
MetaTrader5>=5.0.45

# Optional extras
python-multipart>=0.0.9    # for future file upload support
httpx>=0.27.0              # async HTTP client
